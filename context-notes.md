# 작업 컨텍스트 노트

- 현재 작업 폴더는 `C:\Users\임재명\practice\practice002`이다.
- 사용자가 요청한 작업은 로컬 Git 초기화와 GitHub Public 리포지토리 생성이다.
- 리포지토리 이름은 별도 지정이 없으므로 폴더명 `practice002`를 기준으로 진행한다.
- `gh` 명령은 현재 시스템에서 사용할 수 없는 상태다.
- `git init` 완료 후 기본 브랜치는 `master`로 생성되었다.
